import 'package:flutter/material.dart';

class AddThumbnailPage extends StatelessWidget {
  const AddThumbnailPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Add Thumbnail Page'),
    );
  }
}
