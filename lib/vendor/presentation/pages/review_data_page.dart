import 'package:flutter/material.dart';

class ReviewDataPage extends StatelessWidget {
  const ReviewDataPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Review Data Page'),
    );
  }
}
